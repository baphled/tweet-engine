<h2>Tweet stack</h2>

<% if notice %> 
  <p id="notice"><%= notice %></p>
<% end %>

Currently following: <%= TweetStack.followers %> tweeple
<%= form_tag(tweet_search_phrase_path, :method => :get) do |f| %>
  <%= label_tag(:q, "Search for:") %>
  <%= text_field_tag(:q) %>
  <%= submit_tag("Search") %>
<% end %>


<% if @results.present? %>
  <%= form_tag(tweet_follow_path) do |f| %>
    Tweeple talking about '<%= @term %>':<br />
    <ul>
    <% @results.each_with_index do |name, index| %>
      <li><%= label_tag "followers[name][#{name}]", name %><%= check_box_tag "followers[name][#{name}]", name %></li>
    <% end %>
    </ul>
    <%= submit_tag("Follow") %>
  <% end %>
<% end %>
